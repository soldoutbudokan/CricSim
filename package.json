{
  "name": "cricsim",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "First-person cricket nets practice simulator",
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/game.js && node --check dist/scene.js && node --check dist/physics.js && node --check dist/bat-control.js && node --check dist/audio.js"
  },
  "devDependencies": { "vite": "8.2.2" },
  "engines": { "node": ">=22.12" }
}
